NODE_ENV=production
PORT=5000
CLIENT_URL=https://your-kazov-works-site.vercel.app,http://localhost:5173

MONGO_URI=

JWT_SECRET=
JWT_EXPIRES_IN=
COOKIE_NAME=kazov_admin_token

ADMIN_EMAIL=admin@kazovworks.com
ADMIN_PASSWORD=

CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
CLOUDINARY_FOLDER=kazov-works

# ---------- EMAIL (inquiry form) ----------
# Uses the Resend API to send inquiry emails. RESEND_API_KEY is
# backend-only — it must never be exposed to the frontend or committed.
# EMAIL_FROM must be an address on a domain verified in your Resend
# account (see https://resend.com/domains); until that's configured, the
# inquiry form will fail safely with a generic error rather than send.
RESEND_API_KEY=
EMAIL_FROM=

BUSINESS_EMAIL=samkazokav1181@gmail.com
BUSINESS_PHONE=+919313217425
